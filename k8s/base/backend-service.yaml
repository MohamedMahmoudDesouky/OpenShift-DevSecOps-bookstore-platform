apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: bookstore
  labels:
    app: backend
    tier: backend
spec:
  selector:
    app: backend
    tier: backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
